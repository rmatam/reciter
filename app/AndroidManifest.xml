<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.dailyefforts.android.reviwer"
    android:versionCode="1"
    android:versionName="1.2.5" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Launcher"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".test.TestPage"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".setting.SettingsActivity"
            android:label="@string/settings" >
        </activity>
        <activity
            android:name=".book.WordBookActivity"
            android:label="@string/label_word_book" >
        </activity>

        <provider
            android:name=".db.DbProvider"
            android:authorities="com.gmail.dailyefforts.android.reviwer"
            android:exported="false" >
        </provider>
    </application>

</manifest>
